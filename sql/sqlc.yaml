version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "models"
        out: "models"
        emit_json_tags: true
        emit_enum_valid_method: false